import Title from '@/components/shared/title';
import React from 'react';

const page = () => {
    return (
        <section>
            <Title name={"Borowed books"} />
        </section>
    );
};

export default page;